<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wei.dao.SysUserMapper">
<!--    <insert id="insertBatchSomeColumn">-->
<!--        insert into sys_user (user_id, username, salt, password, email, mobile, status, create_time, del_flag)-->
<!--values-->
<!--        <foreach collection="userList" item="user" separator="," open="(" close=")">-->
<!--            #{user.userId}, #{user.username}, #{user.salt}, #{user.password}, #{user.email}, #{user.mobile}, #{user.status}, #{user.createTime}, #{user.delFlag}-->
<!--        </foreach>-->
<!--    </insert>-->

    <!-- 根据key，更新value -->
    <update id="updateValueByKey" parameterType="map">
        update sys_config set param_value = #{paramValue} where param_key = #{paramKey}
    </update>

    <!-- 根据key，查询value -->
    <select id="queryByKey" parameterType="string" resultType="com.wei.system.sys.SysUserEntity">
        select * from sys_config where param_key = #{paramKey}
    </select>

</mapper>